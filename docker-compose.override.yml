# Local-only overrides (not committed, or committed but never used in CI/CD)
# Enables hot-reload so changes to app/ are picked up without rebuilding.
services:
  app:
    command: uvicorn app.main:app --host 0.0.0.0 --port 8080 --reload
    environment:
      DEBUG: "true"
